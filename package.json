{
  "name": "convector",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel index.html --dist-dir docs --public-url ./",
    "build": "parcel build"
  },
 
  
  "description": "",
  "dependencies": {
    "parcel": "^2.8.3"
  }
}
